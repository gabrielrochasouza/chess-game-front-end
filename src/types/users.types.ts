export interface IUserInfo {
    id: string,
    username: string,
    active: boolean,
    wins: number,
    loses: number,
    draws: number,
    profilePic: string,
    createdAt: string,
    updatedAt: string,
    position: number,
}

